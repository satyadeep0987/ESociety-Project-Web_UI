import { CustomFunctionCategory } from './custom-function-category';

describe('CustomFunctionCategory', () => {
  it('should create an instance', () => {
    expect(new CustomFunctionCategory()).toBeTruthy();
  });
});
