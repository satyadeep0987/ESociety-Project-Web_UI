import { CustomFunctionDetails } from './custom-function-details';

describe('CustomFunctionDetails', () => {
  it('should create an instance', () => {
    expect(new CustomFunctionDetails()).toBeTruthy();
  });
});
