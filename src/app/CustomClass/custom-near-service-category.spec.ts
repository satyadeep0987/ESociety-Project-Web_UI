import { CustomNearServiceCategory } from './custom-near-service-category';

describe('CustomNearServiceCategory', () => {
  it('should create an instance', () => {
    expect(new CustomNearServiceCategory()).toBeTruthy();
  });
});
