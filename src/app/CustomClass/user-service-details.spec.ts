import { UserServiceDetails } from './user-service-details';

describe('UserServiceDetails', () => {
  it('should create an instance', () => {
    expect(new UserServiceDetails()).toBeTruthy();
  });
});
